apply plugin: 'scala'

repositories {
    jcenter()
}

def scalaVersion = '_2.12'

dependencies {
    compile 'org.scala-lang:scala-library:2.12.2'

    testCompile 'junit:junit:4.12'
    testCompile "org.scalatest:scalatest$scalaVersion:3.0.1"
}

